name: Manual Docker Build

on:
    repository_dispatch:
        types: [manual-docker-build]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: Build Docker image
              run: docker build -t chatchat:${{ github.sha }} .
